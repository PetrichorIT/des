include Application

alias PingPongAppl like Application
alias Router like Application

module Main {
    submodules:
        app: some Application
        backup: some Application
}

module A {
    submodules:
        m: Main {
            app = Router,
            backup = Router,
        },
}

